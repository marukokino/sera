<%= form_for(@fabrica) do |f| %>
  <% if @fabrica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fabrica.errors.count, "error") %> prohibited this fabrica from being saved:</h2>

      <ul>
      <% @fabrica.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
<div class="field">
    <%= f.label :cidade %><br>
    <%= f.text_field :cidade %>
  </div>
<div class="field">
    <%= f.label :produtoprincipal %><br>
    <%= f.text_field :produtoprincipal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
